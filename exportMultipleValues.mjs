// Define the array
const myArray = ["camel", 265, true, "5.6"];

// Define the object
const bulb = { watts: 10, type: "LED" };

// Define the function
function multiplyByFour(number) {
    return number * 4;
}

// Export the array, object, and function using named exports
export { myArray, bulb, multiplyByFour };
